# Tasks: station-failure-parks

## 1. Vocabulary + harness stamp

- [x] 1.1 Vocab: register `station.dispatch.failed` (writer `station-harness`, cap run-lifecycle, IntroducedBy this change); vocab census green — PLUS the D2 one-shot marker `station.park.routed` (rule-owned, park-rule writer; the ruleload predicate-declaration gate requires it declared)
- [x] 1.2 Red-first unit pins in `internal/station`: (a) retries-exhausted → the fact lands on the dispatched entity, object names the station + bounded sanitized error (~512 runes), upsert-idempotent (second failure replaces, never appends); (b) SUCCESS path stamps nothing (no failure fact, no success fact); (c) nil-writer tolerance = today's log-only behavior — RED verified (3 pins failed pre-implementation), plus a shutdown-abort pin (Stop-cancelled retries are NOT exhaustion → no stamp, no false terminal)
- [x] 1.3 Implement the stamp in `internal/station/station.go`'s retries-exhausted branch via a new `OwnedFactWriter` DI seam on the station `Config`
- [x] 1.4 Boot wiring: every registered station gets the writer; census enforcement moved INTO `station.New` (rejects a nil FactWriter — stronger than a test: a future station cannot construct writer-less; unit tests direct-construct Component), pinned by `TestNewRejectsNilFactWriter` + the G5 Source tie in `TestToolSourceMatchesVocabWriter`

## 2. Park rules (run-lifecycle pack)

- [x] 2.1 Dispatch-entity census pin: `TestStationDispatchEntityCensus` (test/conformance) — the census CORRECTED D2's expected table: **validation is LOOP-fired** (coordinator/03 fires on the coordinator loop), so run-fired = {projection, provision, delivery}, loop-fired = {validation, floors, verify}. Load-bearing witness: loop-fired dispatches thread `run_entity_id: $entity.triple.agent.run.entity-id` (only resolvable on a loop; the same triple the loop-fired park rule binds). Also pins the park rules' entity-pattern partition (chain vs agentic-loop grammar)
- [x] 2.2 Settled: `graph.mutation.triple.add` (the rule add_triple lane) is an UNCONDITIONAL APPEND (source: graph-ingest `Component.AddTriple` appends under CAS; no per-predicate replace, no exact-triple dedup on this lane). Pinned live by `TestPinGraphAddTripleAppendsDuplicatePredicate` (e2e, real graph lane): duplicate-predicate add → 2 triples. Consequence stated in both rules: one-shot markers are the ONLY re-fire protection; a duplicate park triple is benign
- [x] 2.3 Run-fired park rule `run-lifecycle/05-park-station-failure-run.json` — entity pattern `*.*.agent.chain.execution.*` (the pattern IS the run/loop discrimination; a wildcard would fire on loops where the absence guards pass vacuously), direct guards `run.awaiting.human length_eq 0` + `delivery.pr.ref length_eq 0` + one-shot `station.park.routed`; PARK-FIRST on_enter (review finding: marker-first opened a marker-set-park-absent permanent-stall crash window; park-first makes the worst case a lost notification / benign duplicate) + park message via `$entity.triple.station.dispatch.failed.value` substitution + user-bus publish
- [x] 2.4 Loop-fired park rule `run-lifecycle/06-park-station-failure-loop.json` — entity pattern `*.*.agent.agentic-loop.execution.*`, binds the run via `$entity.triple.agent.run.entity-id` (guarded `ne ""`, the run-less guard), one-shot marker only (cross-entity guards deliberately omitted — racy per the engine's per-action revision semantics; rationale + benign-duplicate trade in-rule)
- [x] 2.5 Bootstrap registration (semdev-bootstrap.json 0.23.0→0.24.0) + ruleload/conformance censuses green; the two rules added to the SANCTIONED park-writer set (`TestOnlySanctionedParkWriters` caught them red-first, as designed)

## 3. The red-first journey (run 1's shape, zero paid tokens)

- [x] 3.1 Mock journey `TestBridgeProofStationFailureParks` (`test/e2e/parks_journey_test.go`): RED verified with the park rules UNREGISTERED (run stalled at requireRunParked after the projection refusal + harness stamp — run 1's exact silent shape, 103s timeout), GREEN with them (parked 14.7s; message names projection-station + the includes-test refusal via substitution; station.dispatch.failed + station.park.routed on the run; no task.spec/verify/delivery; EXACTLY 3 model turns — the dev re-wake stayed gated)
- [x] 3.2 Full `task e2e` green: the new journey + the add-semantics pin + the existing 7 with UNCHANGED RequestCount contracts, `-race`, uncached (205s; prior suite 180s — the delta is the two new tests)

## 4. Verification + review + docs

- [x] 4.1 Full offline ladder (`task check`: build + lint + unit `-race`, zero warnings) + `task e2e` green at head
- [x] 4.2 Adversarial review (go-reviewer + semstreams-reviewer) — BOTH APPROVE, zero blocking/high. ALL findings applied: park-first rule ordering (go MEDIUM — closed the marker-set-park-absent crash window), genuine mid-rune truncate pin (3-byte €, go MEDIUM), exhausted-flag captured at failure time + detached bounded stamp context (go LOW — Stop can neither misclassify nor cancel a real terminal park), de-stutter pin, both-path UTF-8 sanitize, 512-BYTES wording, hint-parameterized requireTriplePresent; spec delta re-worded to the honest run-fired/loop-fired guard split (semstreams MEDIUM — the loop-fired half MAY append a benign duplicate, now its own scenario), resume-must-clear-three-predicates carry-forward NAMED in forge-io-real-lanes design.md (semstreams LOW), the false "add_triple carries no Source" mechanism corrected to the rule_engine-Source/subsystem-label truth (semstreams NIT)
- [x] 4.3 `openspec validate --strict` green
- [x] 4.4 Docs: station.go package doc FAILURE POSTURE rewritten (park named; R8 narrowed to the RESTART half); the exhausted-branch ERROR message now names the stamp+park; stale "no auto-park" claims swept in all six station docs + the R8 tripwire comment (narrowed, tripwire itself unchanged) + the real-LLM journey comment; CLAUDE.md status; evidence-ledger run-1 addendum ("this class now parks", written AFTER the journey went green — G7)
