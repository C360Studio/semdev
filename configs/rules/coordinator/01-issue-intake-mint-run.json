{
  "id": "coordinator_issue_intake_mint_run",
  "type": "expression",
  "name": "Coordinator decide(issue_intake) → mint the run",
  "description": "The run-creation station. When a front-door coordinator loop terminates with decide(action=issue_intake) — the taxonomy token for 'a new, admitted issue needs a run created from it' — mint the run via publish_agent run_scope=new. The framework mints an agent-run rooted at THIS firing coordinator loop (RunID = its own loop id), so the run entity is agent.chain.execution.<coordLoopID> in phase `dispatched`, and stamps agent.loop.run + agent.run.entity-id on this firing entity. run_scope=new requires a publish_agent, so the spawned child is a fresh coordinator wake: the run now exists, and the re-woken coordinator RE-DECIDES the next station (create_change, …). This is the semdev 'coordinator re-decides each wake' pattern (semteams coordinator/30-plan-walking), the analogue of semteams create-change/01's run-minting spawn.\n\nFRONT-DOOR-ONLY GUARD (agent.run.entity-id length_eq 0): issue_intake means 'create a run', so this fires ONLY on a coordinator that has no run anchor yet — the bare front-door wake. It is deliberately absent on a re-woken coordinator (which inherited agent.run.entity-id from this mint), so a re-wake that also lands issue_intake cannot mint a SECOND run. Combined with run_scope=new minting rooted at the firing loop, the guard makes double-mint structurally impossible: the run is minted exactly once, by the first run-less coordinator.\n\nThe re-wake carries tool_choice=required so the coordinator stays on the decide path (semstreams#132/#158 text-only class); its prompt threads the prior decision reason so the re-woken loop has continuity. The re-woken coordinator's full tool/action scoping is finalized when the create_change station lands — at M0 it decides via global tool discovery. The dispatched→executing advance is owned by the agent-run bridge (01-handoff-marker → 02-dispatched-to-executing), never by product Go (G2).",
  "enabled": true,
  "entity": { "pattern": "*.*.*.*.*.*" },
  "conditions": [
    { "field": "agent.loop.role", "operator": "eq", "value": "coordinator", "required": true },
    { "field": "coordinator.decision.next-action", "operator": "eq", "value": "issue_intake", "required": true },
    { "field": "agent.run.entity-id", "operator": "length_eq", "value": 0, "required": true }
  ],
  "logic": "and",
  "on_enter": [
    {
      "type": "publish_agent",
      "run_scope": "new",
      "subject": "agent.task.coordinator",
      "role": "coordinator",
      "model": "coordinator",
      "tools": ["decide", "query_entity", "read_loop_result", "scratchpad"],
      "tool_choice": { "mode": "required" },
      "properties": {
        "run_loop_id": "$entity.instance"
      },
      "related_loops": {
        "issue-intake-run": "$entity.instance"
      },
      "prompt": "SEMDEV COORDINATOR — the run now exists; continue routing the issue→PR arc.\n\nA prior coordinator turn admitted the issue and created this run. Its decision reason: $entity.triple.coordinator.decision.reason\n\nRead the facts recorded for the run so far and choose the single next station of the arc. Record it with the decide tool: one action from your closed taxonomy, plus a short reason. Do not do the work yourself — your job is to route."
    }
  ],
  "on_exit": [],
  "metadata": {
    "pack": "coordinator",
    "capability": "forge-io / run-lifecycle",
    "station": "issue_intake — mint the run",
    "run_entity_pattern": "run_scope=new mints agent.chain.execution.<coordLoopID> (RunID = firing coordinator's loop id) in phase dispatched; agent.loop.run + agent.run.entity-id land on the firing entity; rule.task.spawned lands after the child publish succeeds (the agent-run bridge's handoff trigger).",
    "double_mint_guard": "agent.run.entity-id length_eq 0 — fires only on a run-less front-door coordinator, never a re-wake that inherited the anchor. Also blocks the re-wake (which inherits the anchor) from re-firing this rule, so the spawn chain stops at the child (no spawn storm).",
    "deferred_rewake_scoping": "MUST CLOSE BEFORE THE FIRST REAL-LLM TOKEN (semstreams-reviewer MEDIUM, sharpened by the create_change station): the re-wake carries tool_choice=required with GLOBAL tool discovery, so a real model could call ANY registered tool directly instead of decide — and now that create_change is registered, C2 could stamp the change on its inherited run while bypassing the create_change routing rule (or reach a side-effecting forge-io tool). An action_allowlist does NOT fix this: it only gates the decide tool's action arg, not which tool is called — the fix must SCOPE the re-wake's TOOLS (advertise decide + read tools only, not create_change/forge-io). That needs the bootstrap allowed_tools populated, deferred with this. Inert under the mock (cursor returns decide). Mock-ladder-green is the backstop until then.",
    "deferred_issue_ref": "run.issue.ref stamping lands with the intake component (the issue ref is not a structured triple at rule-firing time); the vocab writer for run.issue.ref moves to this spawn-rule name then."
  }
}
