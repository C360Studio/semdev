# Tasks: forge-io-real-lanes

## 1. Ground truth first (cheap, settles D2/D3)

- [x] 1.1 VERIFIED — the framework webhook input NO LONGER EXISTS (deleted semstreams `a533306e`, beta.147 ADR-075 wave; cutover checklist transfers the receiver + payload shapes + backlog #2/#3 to SEMDEV). D1 grows the receiver half (HTTP+HMAC+flatten+publish onto a semdev-declared GITHUB stream); D3 SETTLED = comment command `/semdev approve` (semdev's OWN flattener carries the issue number + comment author — the gap is ours to close, no upstream ask; task 3.4 dissolves). Full settlement recorded in design.md "As-built settlements"
- [x] 1.2 VERIFIED — engine run_scope=new passes only org/platform/loop-ID to the lifecycle mint (no properties lane to the run; run ID unsubstitutable at mint time) → D2 SETTLED as the loop-entity rule route: bare-ref TaskID (`intake:` prefix dropped, binds nothing) + a `coordinator.decision.next-action eq issue_intake`-gated rule stamping `run.issue.ref` via the run anchor, ref-first + one-shot `run.issue.stamped` marker (park-first lesson). Ruleload/conformance pin lands with the rule (2.3)

## 2. Intake component (the front door goes live)

- [x] 2.1 Component pins (`internal/intake/component_test.go`): admitted → exactly one wake (BaseMessage envelope, BARE-ref TaskID, prompt carries ref+content, nil Tools, msg-id = the admission-record id); rejected → zero publishes/creates + metric + definitive ack (incl. the deliberate allowlist-only-mode checker so a no-token boot rejects definitively instead of retry-looping); malformed → logged ack; redelivered (record exists) → NO double wake; unbound repo → skip. PLUS receiver pins: raw→flat publish keyed by delivery GUID, HMAC enforced (401 + no publish on bad sig), irrelevant events dropped
- [x] 2.2 `issue-intake` component implemented (internal/intake/component.go): BOTH halves — the HTTP receiver (HMAC + filter + semdev-owned flatten + GITHUB-stream publish, msg-id dedup) AND the durable consumer (Normalize → Decide → admission record [births `forge.intake.event` entity: intake.actor.login/admitted + intake.event.ref — the spec's recorded admission + the content-derived idempotency backstop; classified create so EntityExists is a SIGNAL, not swallowed] → wake). Registry entry + `issue-intake-component` alignment note (G1 census caught both red-first)
- [x] 2.3 `run.issue.ref` stamped by `coordinator/04-stamp-issue-ref` (writer moved to issue-ref-rule per the recorded deferred_issue_ref plan, now DISCHARGED in the mint rule's metadata): bare-ref TaskID → agent.loop.task → substituted onto the run via the anchor, gated on the issue_intake decision (can't misfire on rewake/authoring loops), ref-first + one-shot `run.issue.stamped` marker. RED verified (rule unregistered → journey failed at the ref pin, 41.5s), GREEN with it
- [x] 2.4 Bootstrap: GITHUB stream DECLARED (the framework no longer brings one), issue-intake component config (fail-closed defaults: receiver off, repo unbound, empty allowlist), version 0.24.0→0.25.0; boot.RegisterAll registers the component; censuses green
- [x] 2.5 e2e `TestBridgeProofWebhookIssueToApprovedRun` (test/e2e/webhook_journey_test.go): flattened webhook events on the GITHUB stream — NO CoordinatorTask call, NO approveChange stand-in — drive admission → wake → mint → ref → authored+validated → comment approval → resumed → task.spec + cold-proved sandbox. GREEN 15.6s

## 3. Human gates on the issue

- [x] 3.1 Approval adapter (internal/intake/approval.go): `/semdev approve` comment command (two-token whole-word match), attribution = sender==comment-author (Event invariant), authorization via the exported admission-core `Authorize` (opt-in half deliberately not required — approving is a signal on admitted work), run resolved by run.issue.ref over the platform-scoped chain prefix query; pins: authorized releases (exact stand-in triple: run.change.approved=true Source approval-adapter), unauthorized/unattributable ignored, replay idempotent, no-run-yet + resolver faults REDELIVER (the approval-races-mint window), non-command chatter untouched
- [x] 3.2 e2e: folded into `TestBridgeProofWebhookIssueToApprovedRun` station W4 — the gate released from the flattened comment event end-to-end; the stand-in write is absent from the journey
- [x] 3.3 Park-message posting (internal/intake/parkpost.go — the intake component's second durable consumer, USER stream, per D4's G1-minimality): resolves the park publish's firing entity to its run (chain direct / loop via the anchor), reads `run.awaiting.human` + `run.issue.ref`, posts the comment via `github.Client.CreateComment`. Pins: run-fired + loop-fired lanes post to the parsed issue; ref-less/malformed/no-token stay graph-only (definitive ack, loud log); a posting fault REDELIVERS bounded (never blocks the already-durable park); the publish-before-park race redelivers until the message exists
- [x] 3.4 DISSOLVED by the 1.1 finding: the framework retired the flattener, so the payload shape is semdev-OWNED — the comment event now carries the issue number (transferred backlog #2 CLOSED in `internal/forge/githubwebhook`); no upstream ask exists to file. Recorded in the design's as-built settlements

## 4. Real PR delivery (discharges reshape group 8)

- [x] 4.1 `internal/forge/github` additions (pr.go): `FindPRByHead` (state=all — a merged delivery still resolves), `CreatePR`, `CreateComment`; the branch PUSH is a GIT operation, not REST — it lives in the delivery core (`git push <remote> HEAD:refs/heads/semdev/<suffix>`, token injected x-access-token for https only, SANITIZED from every error)
- [x] 4.2 `internal/forge/forgetest` double: records find_pr/create_pr/create_comment/permission requests IN ORDER, serves query-by-head + create + comments; paired with a local BARE git repository so the push is a REAL git push (the double never fakes git)
- [x] 4.3 `openpr.Delivery` (delivery.go): graph guard → push committed branch → query-by-head FIRST → create-on-absence with the evidence-summary body (harness-stamped facts + trajectory pointer) → stamp the REAL PR URL. Unit pins: full order (push,find,create), adopt-existing leg, replay touches NOTHING, failed push aborts pre-API with no token leak, fail-closed on read fault / corrupt ref
- [x] 4.4 The `local-delivery:` stub DELETED (openpr.go); unconfigured forge = loud fail-closed error → the station's retries exhaust → the run PARKS via station-failure-parks (pinned `TestDeliverFailsClosedWithoutForge` + the delivery-station fault pin); EVERY journey now delivers the real path (`injectJourneyForge`: per-test double + bare remote patched into the bootstrap); `requirePRDelivered` asserts the PR URL + evidence body + query-before-create ordering + the REALLY-pushed branch, and the bridge proof gains the LIVE replay station (re-dispatched delivery → still exactly one PR)
- [x] 4.5 Reshape bookkeeping: simplify-m0-execution-rail 8.1/8.2 marked DISCHARGED-BY-forge-io-real-lanes; 8.3 (the recorded real-forge run) stays open, tracked as this change's 5.4

## 5. Verification + review + evidence

- [x] 5.1 Full offline ladder green (`task check` — build + lint + unit `-race`, censuses) + full `task e2e` green `-race` uncached (224s, 10 tests: the 7 prior journeys — RequestCount contracts unchanged, now delivering the REAL path against the double + bare remote — plus the parks journey, the add-semantics pin, and the webhook journey)
- [x] 5.2 Adversarial review — BOTH APPROVE, zero blocking/high (the semstreams reviewer independently re-verified the go findings' fixes in source). ALL findings applied: wake-retry dead-end fixed via run-existence discrimination on ErrAlreadyRecorded (+ recovery pin); delivery pushes the RECORDED attempt.commit.sha fail-closed, never HEAD (+ e2e tip==sha pin); consumer MaxDeliver 3→10 with deliberate MessageTimeout/heartbeat (the approval-races-mint drop); the e2e replay pin discriminates on the double's REQUEST LOG (zero-new-calls) instead of a sleep; percent-encoded token sanitize; tokenless-https boot warning; delivery schema exposes the forge block (+ embedding pin); MaxBytesReader/ReadTimeout on the receiver; honest GitHub-manual-redeliver comment; approval value check ("true", not presence); entity_not_found classified → definitive ack in the park poster; stale approveChange stand-in label corrected; GITHUB stream duplicates window 10m; two LOWs tracked as named design follow-ups (git-argv token, park-post wire test)
- [x] 5.3 `openspec validate --strict` green
- [x] 5.4 DONE (2026-07-20 — M0-COMPLETION CLAIMED): ONE recorded real-forge delivery — PR
  https://github.com/C360Studio/semdev-test/pull/2 (the same live run that closed self-target 7.4:
  real Gemini all-roles against a seeded disposable repo, cloned from its coordinate, cold-verified,
  fix-alone diff delivered to main; pull-first `semdev launch` + stand-in `/semdev approve`). Evidence:
  docs/evidence-ledger.md M0-completion entry. (Originally: OPERATOR-GATED — the operator picks the
  target per reshape 8.3; the M0-completion G7 requirement.)
